// Generated by CoffeeScript 1.6.3
var https, remoteCoffee, vm;

https = require("https");

vm = require("vm");

remoteCoffee = require("remote-coffee");

exports.load = function(hotModule) {
  return remoteCoffee(hotModule.codeUrl, function(moduleSourceCode) {
    var context, hotCode;
    hotCode = vm.createScript(moduleSourceCode, hotModule.codeUrl);
    context = vm.createContext({
      Markmotron: Markmotron,
      Stackmotron: Stackmotron,
      console: console,
      setTimeout: setTimeout,
      setInterval: setInterval,
      clearTimeout: clearTimeout,
      require: require,
      env: process.env
    });
    hotCode.runInContext(context);
    return console.log("Running Script: ", hotModule.codeUrl);
  });
};
